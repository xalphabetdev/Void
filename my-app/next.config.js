// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true, // This can be set to false if not needed
    images: {
      domains: ['aceternity.com'], // Add any other domains if necessary
    },
  };
  
  module.exports = nextConfig;
  